# munich-kvr-pass-pickup
A tiny python script to automatically check whether the passport / ID pick up is ready at the KVR after ordering a new one.
The script fetches the API with a given pick up number.
